# Frontend (safe to expose — embed directly, Vite requires the VITE_ prefix)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (set these in Vercel project settings, never commit real values)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SESSION_SECRET=
